Sistema de Login e Cadastro (Java Console)
Este é um projeto simples, desenvolvido em Java, que simula um sistema básico de login, cadastro e exclusão de contas rodando totalmente via Terminal (Console). Foi criado com o objetivo de reforçar os fundamentos da programação procedural e o uso de estruturas de controle essenciais, antes de migrar para conceitos avançados como Orientação a Objetos (POO).

💡 Conceitos em Destaque
Este projeto demonstra o uso prático de três pilares da programação procedural:

Loop de Controle (while): Utilizado para manter o menu principal ativo e permitir que o usuário realize múltiplas operações (login, cadastro, exclusão) sem sair do programa.

Estrutura de Escolha (switch): Usada para interpretar a opção numérica do usuário (1, 2, 3 ou 4) e direcionar o fluxo do programa para a lógica específica.

Loop de Validação (do-while): Implementado dentro da lógica de login para forçar o usuário a digitar as credenciais repetidamente até que o acesso seja concedido.

⚙️ Como Executar o Projeto
Para rodar este sistema em seu ambiente local, você precisa ter o JDK (Java Development Kit) instalado.

Pré-requisitos
Java (JDK): Versão 8 ou superior.

Editor de Código: Recomendado o VS Code com a extensão Java.
